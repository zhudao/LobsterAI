engine-strict=true
; better-sqlite3 13.0.3 ships prebuilt N-API binaries. package.json uses
; allowScripts to skip npm's implicit node-gyp rebuild when loading a lockfile.
; A node-gyp override is insufficient: npm 11.17 does not forward it when
; running dependency lifecycle scripts. Keep the npm engine requirement in sync.
