diff --git a/src/agents/embedded-agent-runner/replay-history.ts b/src/agents/embedded-agent-runner/replay-history.ts
index f7e35606..29f1524a 100644
--- a/src/agents/embedded-agent-runner/replay-history.ts
+++ b/src/agents/embedded-agent-runner/replay-history.ts
@@ -74,6 +74,9 @@ import {
 } from "./thinking.js";

 const MODEL_SNAPSHOT_CUSTOM_TYPE = "model-snapshot";
+const MAX_PRESERVED_ABORTED_TOOL_HISTORY_PAIRS = 3;
+const ABORTED_TOOL_HISTORY_TEXT_RE =
+  /^(?:aborted|operation (?:was )?aborted|request was aborted|cancelled|canceled)$/i;
 type CustomEntryLike = { type?: unknown; customType?: unknown; data?: unknown };
 type ModelSnapshotEntry = {
   timestamp: number;
@@ -638,6 +641,143 @@ function createProviderReplaySessionState(
   };
 }

+function getMessageRole(message: AgentMessage | undefined): string | undefined {
+  if (!message || typeof message !== "object") {
+    return undefined;
+  }
+  return (message as { role?: unknown }).role as string | undefined;
+}
+
+function getTextFromContent(content: unknown): string {
+  if (typeof content === "string") {
+    return content;
+  }
+  if (!Array.isArray(content)) {
+    return "";
+  }
+  return content
+    .map((block) => {
+      if (typeof block === "string") {
+        return block;
+      }
+      if (!block || typeof block !== "object") {
+        return "";
+      }
+      const text = (block as { text?: unknown }).text;
+      return typeof text === "string" ? text : "";
+    })
+    .join("\n")
+    .trim();
+}
+
+function hasAssistantVisibleText(message: Extract<AgentMessage, { role: "assistant" }>): boolean {
+  return getTextFromContent((message as { content?: unknown }).content).trim().length > 0;
+}
+
+function getToolOnlyAssistantCallIds(message: AgentMessage): Set<string> | null {
+  if (getMessageRole(message) !== "assistant") {
+    return null;
+  }
+  const assistant = message as Extract<AgentMessage, { role: "assistant" }>;
+  if (hasAssistantVisibleText(assistant)) {
+    return null;
+  }
+  const toolCalls = extractToolCallsFromAssistant(assistant);
+  if (toolCalls.length === 0) {
+    return null;
+  }
+  return new Set(toolCalls.map((call) => call.id).filter(Boolean));
+}
+
+function extractReplayToolResultId(message: AgentMessage): string | null {
+  if (getMessageRole(message) === "toolResult") {
+    return extractToolResultId(message as Extract<AgentMessage, { role: "toolResult" }>);
+  }
+  const record = message as unknown as Record<string, unknown>;
+  for (const value of [
+    record.toolUseId,
+    record.toolCallId,
+    record.tool_use_id,
+    record.tool_call_id,
+    record.callId,
+    record.call_id,
+  ]) {
+    if (typeof value === "string" && value.trim()) {
+      return value.trim();
+    }
+  }
+  return null;
+}
+
+function isAbortedToolResultMessage(message: AgentMessage): boolean {
+  const role = getMessageRole(message);
+  if (role !== "toolResult" && role !== "tool") {
+    return false;
+  }
+  return ABORTED_TOOL_HISTORY_TEXT_RE.test(
+    getTextFromContent((message as { content?: unknown }).content).trim(),
+  );
+}
+
+function sanitizeAbortedToolLoopHistory(messages: AgentMessage[]): AgentMessage[] {
+  const pairs: Array<{ start: number; endExclusive: number }> = [];
+
+  for (let index = 0; index < messages.length; index += 1) {
+    const message = messages[index];
+    if (!message) {
+      continue;
+    }
+    const expectedIds = getToolOnlyAssistantCallIds(message);
+    if (!expectedIds || expectedIds.size === 0) {
+      continue;
+    }
+
+    const seenIds = new Set<string>();
+    let nextIndex = index + 1;
+    let toolResultCount = 0;
+    let allResultsAborted = true;
+    while (nextIndex < messages.length) {
+      const candidate = messages[nextIndex];
+      if (!candidate) {
+        break;
+      }
+      const role = getMessageRole(candidate);
+      if (role !== "toolResult" && role !== "tool") {
+        break;
+      }
+      toolResultCount += 1;
+      if (!isAbortedToolResultMessage(candidate)) {
+        allResultsAborted = false;
+        break;
+      }
+      const toolResultId = extractReplayToolResultId(candidate);
+      if (toolResultId) {
+        seenIds.add(toolResultId);
+      }
+      nextIndex += 1;
+    }
+
+    const allExpectedIdsSeen = [...expectedIds].every((id) => seenIds.has(id));
+    if (toolResultCount > 0 && allResultsAborted && allExpectedIdsSeen) {
+      pairs.push({ start: index, endExclusive: nextIndex });
+      index = nextIndex - 1;
+    }
+  }
+
+  if (pairs.length <= MAX_PRESERVED_ABORTED_TOOL_HISTORY_PAIRS) {
+    return messages;
+  }
+
+  const dropIndexes = new Set<number>();
+  for (const pair of pairs.slice(0, -MAX_PRESERVED_ABORTED_TOOL_HISTORY_PAIRS)) {
+    for (let index = pair.start; index < pair.endExclusive; index += 1) {
+      dropIndexes.add(index);
+    }
+  }
+
+  return messages.filter((_, index) => !dropIndexes.has(index));
+}
+
 function readModelSnapshotState(sessionManager: SessionManager): ModelSnapshotState {
   let lastSnapshot: ModelSnapshotEntry | null = null;
   let latestSwitchTimestamp: number | null = null;
@@ -815,8 +955,9 @@ export async function sanitizeSessionHistory(params: {
     ? currentSnapshot?.timestamp
     : snapshotState.latestSwitchTimestamp;
   const normalizedAssistantReplay = normalizeAssistantReplayContent(withInterSessionMarkers);
+  const sanitizedAbortedToolLoops = sanitizeAbortedToolLoopHistory(normalizedAssistantReplay);
   const sanitizedImages = await sanitizeSessionMessagesImages(
-    normalizedAssistantReplay,
+    sanitizedAbortedToolLoops,
     "session:history",
     {
       sanitizeMode: policy.sanitizeMode,
diff --git a/src/agents/embedded-agent-runner/sanitize-session-history.tool-result-details.test.ts b/src/agents/embedded-agent-runner/sanitize-session-history.tool-result-details.test.ts
index 99684f74..3c07fb30 100644
--- a/src/agents/embedded-agent-runner/sanitize-session-history.tool-result-details.test.ts
+++ b/src/agents/embedded-agent-runner/sanitize-session-history.tool-result-details.test.ts
@@ -20,6 +20,51 @@ vi.mock("../../plugins/provider-hook-runtime.js", () => ({
 }));

 describe("sanitizeSessionHistory toolResult details stripping", () => {
+  it("drops old aborted tool-only turns while preserving recent diagnostic pairs", async () => {
+    const sm = SessionManager.inMemory();
+
+    const messages: AgentMessage[] = [];
+    for (let i = 0; i < 10; i += 1) {
+      messages.push(
+        makeAgentAssistantMessage({
+          content: [
+            { type: "toolCall", id: `call_${i}`, name: "exec", arguments: { command: "dir" } },
+          ],
+          model: "gpt-5.4",
+          stopReason: "toolUse",
+          timestamp: i * 2,
+        }),
+        {
+          role: "toolResult",
+          toolCallId: `call_${i}`,
+          toolName: "exec",
+          isError: true,
+          content: [{ type: "text", text: "Aborted" }],
+          timestamp: i * 2 + 1,
+        } as AgentMessage,
+      );
+    }
+    messages.push({
+      role: "user",
+      content: "continue",
+      timestamp: 30,
+    } satisfies UserMessage);
+
+    const sanitized = await sanitizeSessionHistory({
+      messages,
+      modelApi: "anthropic-messages",
+      provider: "anthropic",
+      modelId: "claude-opus-4-6",
+      sessionManager: sm,
+      sessionId: "test",
+    });
+
+    expect(sanitized).toHaveLength(7);
+    expect(sanitized.map((message) => message.timestamp)).toEqual([
+      14, 15, 16, 17, 18, 19, 30,
+    ]);
+  });
+
   it("strips toolResult.details so untrusted payloads are not fed back to the model", async () => {
     // details can contain raw tool metadata or untrusted data; only normalized
     // tool content should be replayed to the model.
diff --git a/src/agents/tool-loop-detection.test.ts b/src/agents/tool-loop-detection.test.ts
index ee03295d..1ce3287e 100644
--- a/src/agents/tool-loop-detection.test.ts
+++ b/src/agents/tool-loop-detection.test.ts
@@ -16,6 +16,8 @@ vi.mock("./embedded-agent-messaging.js", () => ({
 }));
 import { reconcileToolCallExecutionParams } from "./tool-loop-call-reconciliation.js";
 import {
+  ABORTED_TOOL_LOOP_CRITICAL_THRESHOLD,
+  ABORTED_TOOL_LOOP_TOTAL_THRESHOLD,
   UNKNOWN_TOOL_THRESHOLD,
   detectToolCallLoop,
   recordToolCall,
@@ -131,6 +133,17 @@ function createNoProgressPollFixture(sessionId: string) {
   };
 }

+function createAbortedExecFixture() {
+  return {
+    toolName: "exec",
+    params: { command: "dir" },
+    result: {
+      content: [{ type: "text", text: "Aborted" }],
+      details: { ok: false },
+    },
+  } as const;
+}
+
 function createReadNoProgressFixture() {
   return {
     toolName: "read",
@@ -449,6 +462,54 @@ describe("tool-loop-detection", () => {
       }
     });

+    it("blocks repeated aborted tool results before the generic critical threshold", () => {
+      const fixture = createAbortedExecFixture();
+      const loopResult = detectLoopAfterRepeatedCalls({
+        toolName: fixture.toolName,
+        toolParams: fixture.params,
+        result: fixture.result,
+        count: ABORTED_TOOL_LOOP_CRITICAL_THRESHOLD,
+      });
+
+      expect(loopResult.stuck).toBe(true);
+      if (loopResult.stuck) {
+        expect(loopResult.level).toBe("critical");
+        expect(loopResult.detector).toBe("aborted_tool_loop");
+        expect(loopResult.message).toContain("aborted outcomes");
+      }
+    });
+
+    it("blocks excessive aborted results across different signatures", () => {
+      const state = createState();
+
+      for (let index = 0; index < ABORTED_TOOL_LOOP_TOTAL_THRESHOLD; index += 1) {
+        recordSuccessfulCall(
+          state,
+          "exec",
+          { command: `dir ${index}` },
+          {
+            content: [{ type: "text", text: "Operation aborted" }],
+            details: { ok: false },
+          },
+          index,
+        );
+      }
+
+      const loopResult = detectToolCallLoop(
+        state,
+        "exec",
+        { command: "dir final" },
+        enabledLoopDetectionConfig,
+      );
+
+      expect(loopResult.stuck).toBe(true);
+      if (loopResult.stuck) {
+        expect(loopResult.level).toBe("critical");
+        expect(loopResult.detector).toBe("aborted_tool_loop");
+        expect(loopResult.message).toContain("aborted outcomes");
+      }
+    });
+
     it.each(["thrown", "returned", 0, 1, 2, 3, 4] as const)(
       "blocks repeated external outcomes with fresh nonces (%s)",
       (shape) => {
diff --git a/src/agents/tool-loop-detection.ts b/src/agents/tool-loop-detection.ts
index ad173fe4..271854bc 100644
--- a/src/agents/tool-loop-detection.ts
+++ b/src/agents/tool-loop-detection.ts
@@ -30,6 +30,7 @@ type LoopDetectorKind =
   | "argument_churn"
   | "unknown_tool_repeat"
   | "known_poll_no_progress"
+  | "aborted_tool_loop"
   | "global_circuit_breaker"
   | "ping_pong";

@@ -50,6 +51,11 @@ const TOOL_CALL_HISTORY_SIZE = 30;
 export const UNKNOWN_TOOL_THRESHOLD = 10;
 const CRITICAL_THRESHOLD = 20;
 const GLOBAL_CIRCUIT_BREAKER_THRESHOLD = 30;
+export const ABORTED_TOOL_LOOP_WARNING_THRESHOLD = 5;
+export const ABORTED_TOOL_LOOP_CRITICAL_THRESHOLD = 8;
+export const ABORTED_TOOL_LOOP_TOTAL_THRESHOLD = 20;
+const ABORTED_OUTCOME_RE =
+  /\b(?:aborterror|aborted|operation aborted|request was aborted|operation was aborted|cancelled|canceled)\b/i;

 type ToolLoopDetectionScope = {
   runId?: string;
@@ -286,6 +292,18 @@ function isLoopVetoResult(details: Record<string, unknown>): boolean {
   return details.status === "blocked" && details.deniedReason === "tool-loop";
 }

+function normalizeAbortedOutcomeText(text: string): string | undefined {
+  const normalized = text.replace(/\s+/g, " ").trim();
+  if (!normalized || !ABORTED_OUTCOME_RE.test(normalized)) {
+    return undefined;
+  }
+  return "aborted";
+}
+
+function isAbortedOutcomeHash(resultHash?: string): boolean {
+  return typeof resultHash === "string" && resultHash.startsWith("aborted:");
+}
+
 type ToolCallOutcome = Pick<
   ToolCallRecord,
   "failureIdentityHash" | "outcomeKind" | "resultHash" | "noProgress" | "unknownToolName"
@@ -299,14 +317,25 @@ function hashToolOutcome(
 ): ToolCallOutcome {
   if (error !== undefined) {
     const unknownToolName = extractUnknownToolName(error);
+    const errorText = formatErrorForHash(error);
+    const abortedOutcome = normalizeAbortedOutcomeText(errorText);
     return {
-      resultHash: `error:${digestToolOutcome(formatErrorForHash(error))}`,
+      resultHash: abortedOutcome ? `aborted:${abortedOutcome}` : `error:${digestToolOutcome(errorText)}`,
       noProgress: true,
       unknownToolName,
     };
   }
   if (!isPlainObject(result)) {
-    return { resultHash: result === undefined ? undefined : digestToolOutcome(result) };
+    if (result === undefined) {
+      return { resultHash: undefined };
+    }
+    if (typeof result === "string") {
+      const abortedOutcome = normalizeAbortedOutcomeText(result);
+      if (abortedOutcome) {
+        return { resultHash: `aborted:${abortedOutcome}`, noProgress: true };
+      }
+    }
+    return { resultHash: digestToolOutcome(result) };
   }

   const details = isPlainObject(result.details) ? result.details : {};
@@ -316,6 +345,10 @@ function hashToolOutcome(
   if (isLoopVetoResult(details)) {
     return { outcomeKind: "tool-loop-veto" };
   }
+  const abortedOutcome = normalizeAbortedOutcomeText(text);
+  if (abortedOutcome) {
+    return { resultHash: `aborted:${abortedOutcome}`, noProgress: true };
+  }
   if (toolName === "exec") {
     const execHash = hashExecToolOutcome(details, text);
     if (execHash) {
@@ -516,6 +549,36 @@ function canonicalPairKey(signatureA: string, signatureB: string): string {
   return [signatureA, signatureB].toSorted().join("|");
 }

+function getAbortedToolLoopStats(
+  history: readonly ToolCallRecord[],
+  toolName: string,
+  argsHash: string,
+): { signatureStreak: number; total: number } {
+  let signatureStreak = 0;
+  let total = 0;
+  let signatureStreakOpen = true;
+
+  for (let index = history.length - 1; index >= 0; index -= 1) {
+    const record = history[index];
+    if (!record) {
+      continue;
+    }
+    const aborted = isAbortedOutcomeHash(record.resultHash);
+    if (aborted) {
+      total += 1;
+    }
+    if (signatureStreakOpen && record.toolName === toolName && record.argsHash === argsHash) {
+      if (!aborted) {
+        signatureStreakOpen = false;
+      } else {
+        signatureStreak += 1;
+      }
+    }
+  }
+
+  return { signatureStreak, total };
+}
+
 /**
  * Detect if an agent is stuck in a repetitive tool call loop.
  * Checks if the same tool+params combination has been called excessively.
@@ -537,6 +600,7 @@ export function detectToolCallLoop(
   const noProgressStreak = noProgress.count;
   const argumentChurn = getArgumentChurnNoProgressStreak(history, toolName, currentHash);
   const knownPollTool = isKnownPollToolCall(toolName, params);
+  const abortedLoop = getAbortedToolLoopStats(history, toolName, currentHash);
   const pingPong = getPingPongStreak(history, currentHash);
   const argumentChurnLivenessSignal =
     argumentChurn.count >= TOOL_LOOP_WARNING_THRESHOLD ? ("argument_churn" as const) : undefined;
@@ -566,6 +630,48 @@ export function detectToolCallLoop(
     };
   }

+  if (abortedLoop.total >= ABORTED_TOOL_LOOP_TOTAL_THRESHOLD) {
+    log.error(
+      `Aborted tool loop breaker triggered: ${abortedLoop.total} aborted tool outcomes recorded`,
+    );
+    return {
+      stuck: true,
+      level: "critical",
+      detector: "aborted_tool_loop",
+      count: abortedLoop.total,
+      message: `CRITICAL: tool calls have returned aborted outcomes ${abortedLoop.total} times. Session execution blocked to prevent runaway retry loops.`,
+      warningKey: `aborted-total:${abortedLoop.total}`,
+    };
+  }
+
+  if (abortedLoop.signatureStreak >= ABORTED_TOOL_LOOP_CRITICAL_THRESHOLD) {
+    log.error(
+      `Critical aborted tool loop detected: ${toolName} repeated ${abortedLoop.signatureStreak} times`,
+    );
+    return {
+      stuck: true,
+      level: "critical",
+      detector: "aborted_tool_loop",
+      count: abortedLoop.signatureStreak,
+      message: `CRITICAL: ${toolName} has returned aborted outcomes ${abortedLoop.signatureStreak} times for identical arguments. Session execution blocked to prevent runaway retry loops.`,
+      warningKey: `aborted:${toolName}:${currentHash}:${noProgress.latestResultHash ?? "aborted"}`,
+    };
+  }
+
+  if (abortedLoop.signatureStreak >= ABORTED_TOOL_LOOP_WARNING_THRESHOLD) {
+    log.warn(
+      `Aborted tool loop warning: ${toolName} repeated ${abortedLoop.signatureStreak} times`,
+    );
+    return {
+      stuck: true,
+      level: "warning",
+      detector: "aborted_tool_loop",
+      count: abortedLoop.signatureStreak,
+      message: `WARNING: ${toolName} has returned aborted outcomes ${abortedLoop.signatureStreak} times for identical arguments. Stop retrying the same call and choose a different approach or report the task as failed.`,
+      warningKey: `aborted:${toolName}:${currentHash}:${noProgress.latestResultHash ?? "aborted"}`,
+    };
+  }
+
   if (knownPollTool && noProgressStreak >= CRITICAL_THRESHOLD) {
     log.error(`Critical polling loop detected: ${toolName} repeated ${noProgressStreak} times`);
     return {
diff --git a/src/infra/diagnostic-events.ts b/src/infra/diagnostic-events.ts
index 0f14a03b..336c1027 100644
--- a/src/infra/diagnostic-events.ts
+++ b/src/infra/diagnostic-events.ts
@@ -477,6 +477,7 @@ export type DiagnosticToolLoopEvent = DiagnosticBaseEvent & {
     | "argument_churn"
     | "unknown_tool_repeat"
     | "known_poll_no_progress"
+    | "aborted_tool_loop"
     | "global_circuit_breaker"
     | "ping_pong";
   count: number;
diff --git a/src/logging/diagnostic.ts b/src/logging/diagnostic.ts
index 723c96cd..19611cd6 100644
--- a/src/logging/diagnostic.ts
+++ b/src/logging/diagnostic.ts
@@ -1110,6 +1110,7 @@ export function logToolLoopAction(
       | "argument_churn"
       | "unknown_tool_repeat"
       | "known_poll_no_progress"
+      | "aborted_tool_loop"
       | "global_circuit_breaker"
       | "ping_pong";
     count: number;
