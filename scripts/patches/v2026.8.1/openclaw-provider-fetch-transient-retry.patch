diff --git a/src/agents/provider-transport-fetch.test.ts b/src/agents/provider-transport-fetch.test.ts
index 81e9a37b..e8cd4323 100644
--- a/src/agents/provider-transport-fetch.test.ts
+++ b/src/agents/provider-transport-fetch.test.ts
@@ -758,6 +758,60 @@ describe("buildGuardedModelFetch", () => {
     expect(release).toHaveBeenCalledTimes(1);
   });

+  it("retries a replayable request once after a transient transport failure", async () => {
+    const transient = new TypeError("fetch failed") as TypeError & {
+      cause?: { name: string; code: string };
+    };
+    transient.cause = { name: "SocketError", code: "UND_ERR_SOCKET" };
+    const release = vi.fn(async () => undefined);
+    fetchWithSsrFGuardMock
+      .mockRejectedValueOnce(transient)
+      .mockResolvedValueOnce({
+        response: new Response('{"ok":true}', {
+          status: 200,
+          headers: { "content-type": "application/json" },
+        }),
+        finalUrl: "https://api.openai.com/v1/chat/completions",
+        release,
+      });
+    const model = makeProviderModelFixture<"openai-completions">({
+      id: "gpt-5.4",
+      provider: "openai",
+      api: "openai-completions",
+      baseUrl: "https://api.openai.com/v1",
+    });
+
+    const response = await buildGuardedModelFetch(model)(
+      "https://api.openai.com/v1/chat/completions",
+      { method: "POST", body: '{"stream":true}' },
+    );
+
+    expect(await response.text()).toContain('"ok":true');
+    expect(fetchWithSsrFGuardMock).toHaveBeenCalledTimes(2);
+  });
+
+  it("does not retry a transient failure when the body cannot be replayed", async () => {
+    const transient = new TypeError("fetch failed") as TypeError & {
+      cause?: { name: string; code: string };
+    };
+    transient.cause = { name: "SocketError", code: "UND_ERR_SOCKET" };
+    fetchWithSsrFGuardMock.mockRejectedValue(transient);
+    const model = makeProviderModelFixture<"openai-completions">({
+      id: "gpt-5.4",
+      provider: "openai",
+      api: "openai-completions",
+      baseUrl: "https://api.openai.com/v1",
+    });
+
+    await expect(
+      buildGuardedModelFetch(model)("https://api.openai.com/v1/chat/completions", {
+        method: "POST",
+        body: new ReadableStream<Uint8Array>(),
+      } as RequestInit),
+    ).rejects.toThrow("fetch failed");
+    expect(fetchWithSsrFGuardMock).toHaveBeenCalledTimes(1);
+  });
+
   it("scopes fake-IP DNS exemptions to the configured provider host", async () => {
     const model = makeProviderModelFixture<"openai-responses">({
       id: "gpt-5.4",
diff --git a/src/agents/provider-transport-fetch.ts b/src/agents/provider-transport-fetch.ts
index 4b0f154e..a4ea0af0 100644
--- a/src/agents/provider-transport-fetch.ts
+++ b/src/agents/provider-transport-fetch.ts
@@ -54,6 +54,7 @@ import {
 import { getProviderTransportDispatcherPool } from "./provider-transport-dispatcher-pool.js";

 const DEFAULT_MAX_SDK_RETRY_WAIT_SECONDS = 60;
+const TRANSIENT_PROVIDER_FETCH_RETRY_DELAY_MS = 750;
 const OPENAI_SDK_STREAM_CONTENT_SNIFF_BYTES = 2 * 1024;
 const log = createSubsystemLogger("provider-transport-fetch");

@@ -70,6 +71,69 @@ const SSE_SANITIZE_BUFFER_MAX_CHARS = 16 * 1024 * 1024;

 const BLOCKED_EXACT_ORIGIN_TRUST_HOSTNAME_LABELS = new Set(["instance-data"]);
 const PLAIN_DECIMAL_NUMBER_RE = /^\d+(?:\.\d+)?$/;
+const TRANSIENT_PROVIDER_FETCH_ERROR_CODES = new Set([
+  "ECONNRESET",
+  "ECONNABORTED",
+  "EPIPE",
+  "ETIMEDOUT",
+  "UND_ERR_BODY_TIMEOUT",
+  "UND_ERR_CONNECT_TIMEOUT",
+  "UND_ERR_HEADERS_TIMEOUT",
+  "UND_ERR_SOCKET",
+]);
+
+function readErrorStringField(error: unknown, key: "code" | "message" | "name") {
+  if (!error || typeof error !== "object") {
+    return undefined;
+  }
+  const value = (error as Record<string, unknown>)[key];
+  return typeof value === "string" && value.trim() ? value.trim() : undefined;
+}
+
+function isTransientProviderFetchTransportError(error: unknown): boolean {
+  const cause = error && typeof error === "object" ? (error as { cause?: unknown }).cause : undefined;
+  const code = readErrorStringField(error, "code") ?? readErrorStringField(cause, "code");
+  if (code && TRANSIENT_PROVIDER_FETCH_ERROR_CODES.has(code)) {
+    return true;
+  }
+  const causeName = readErrorStringField(cause, "name");
+  if (causeName === "ConnectTimeoutError" || causeName === "SocketError") {
+    return true;
+  }
+  const message = error instanceof Error ? error.message : (readErrorStringField(error, "message") ?? "");
+  return (
+    /\b(?:socket hang up|connection reset|connection aborted|fetch failed)\b/i.test(message) &&
+    (causeName === "ConnectTimeoutError" || causeName === "SocketError" || Boolean(code))
+  );
+}
+
+function isReplayableProviderFetchBody(body: BodyInit | null | undefined): boolean {
+  return (
+    body === undefined ||
+    body === null ||
+    typeof body === "string" ||
+    body instanceof URLSearchParams ||
+    body instanceof Blob ||
+    body instanceof FormData ||
+    body instanceof ArrayBuffer ||
+    ArrayBuffer.isView(body)
+  );
+}
+
+function shouldRetryProviderFetch(error: unknown, init: RequestInit | undefined): boolean {
+  return (
+    init?.signal?.aborted !== true &&
+    isReplayableProviderFetchBody(init?.body as BodyInit | null | undefined) &&
+    isTransientProviderFetchTransportError(error)
+  );
+}
+
+function delayProviderFetchRetry(): Promise<void> {
+  return new Promise((resolve) => {
+    const timer = setTimeout(resolve, TRANSIENT_PROVIDER_FETCH_RETRY_DELAY_MS);
+    timer.unref?.();
+  });
+}

 function hasReadableSseData(block: string): boolean {
   const dataLines = block
@@ -916,11 +980,25 @@ export function buildGuardedModelFetch(
         rawHeaders,
         localServiceSignal,
       );
-      result = await fetchWithSsrFGuard(
-        useEnvProxy
-          ? withTrustedEnvProxyGuardedFetchMode(guardedFetchOptions)
-          : guardedFetchOptions,
-      );
+      for (let attempt = 1; ; attempt += 1) {
+        try {
+          result = await fetchWithSsrFGuard(
+            useEnvProxy
+              ? withTrustedEnvProxyGuardedFetchMode(guardedFetchOptions)
+              : guardedFetchOptions,
+          );
+          break;
+        } catch (error) {
+          if (attempt >= 2 || !shouldRetryProviderFetch(error, baseInit)) {
+            throw error;
+          }
+          log.warn(
+            `[model-fetch] transient transport failure; retrying provider=${model.provider} ` +
+              `api=${model.api} model=${model.id} attempt=${attempt + 1} ${summarizeError(error)}`,
+          );
+          await delayProviderFetchRetry();
+        }
+      }
     } catch (error) {
       const remediatedError = withModelProviderNetworkRemediation(error, {
         baseUrl: model.baseUrl,
