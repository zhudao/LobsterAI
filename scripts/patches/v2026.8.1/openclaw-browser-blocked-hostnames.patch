diff --git a/extensions/browser/src/browser/config.test.ts b/extensions/browser/src/browser/config.test.ts
index 3062d64f..453d7d84 100644
--- a/extensions/browser/src/browser/config.test.ts
+++ b/extensions/browser/src/browser/config.test.ts
@@ -683,6 +683,7 @@ describe("browser config", () => {
           allowRfc2544BenchmarkRange: true,
           allowIpv6UniqueLocalRange: true,
           allowedHostnames: [" localhost ", " *.trusted.example ", ""],
+          blockedHostnames: [" www.baidu.com ", ""],
         },
       } as unknown as BrowserConfig,
       expected: {
@@ -690,6 +691,7 @@ describe("browser config", () => {
         allowRfc2544BenchmarkRange: true,
         allowIpv6UniqueLocalRange: true,
         allowedHostnames: ["localhost", "*.trusted.example"],
+        blockedHostnames: ["www.baidu.com"],
       },
     },
     {
diff --git a/extensions/browser/src/browser/config.ts b/extensions/browser/src/browser/config.ts
index 0d6ff040..127fd4e1 100644
--- a/extensions/browser/src/browser/config.ts
+++ b/extensions/browser/src/browser/config.ts
@@ -249,6 +249,7 @@ function resolveBrowserSsrFPolicy(cfg: BrowserConfig | undefined): SsrFPolicy |
   const resolved = mergeSsrFPolicies({
     ...rawPolicy,
     allowedHostnames: normalizeStringList(rawPolicy?.allowedHostnames),
+    blockedHostnames: normalizeStringList(rawPolicy?.blockedHostnames),
   });
   if (resolved && hasExplicitPrivateSetting) {
     delete resolved.allowPrivateNetwork;
diff --git a/src/config/schema.help.runtime.ts b/src/config/schema.help.runtime.ts
index 4de31236..04d88082 100644
--- a/src/config/schema.help.runtime.ts
+++ b/src/config/schema.help.runtime.ts
@@ -61,6 +61,8 @@ export const RUNTIME_FIELD_HELP: Record<string, string> = {
     "Allows access to private-network address ranges from browser tooling. Default is disabled when unset; enable only for explicitly trusted private-network destinations.",
   "browser.ssrfPolicy.allowedHostnames":
     "Exact hostnames or IP literals allowed by browser SSRF policy checks. Keep the list minimal.",
+  "browser.ssrfPolicy.blockedHostnames":
+    'Hostname blocklist patterns for browser navigation, supporting exact hosts and "*.example.com" wildcard subdomains.',
   "browser.ssrfPolicy.allowRfc2544BenchmarkRange":
     "Allow RFC 2544 benchmark-range IPs (198.18.0.0/15) for trusted fake-IP proxy environments.",
   "browser.ssrfPolicy.allowIpv6UniqueLocalRange":
diff --git a/src/config/schema.labels.ts b/src/config/schema.labels.ts
index 37bc09a6..415c4c3b 100644
--- a/src/config/schema.labels.ts
+++ b/src/config/schema.labels.ts
@@ -741,6 +741,7 @@ export const FIELD_LABELS: Record<string, string> = {
   "browser.ssrfPolicy": "Browser SSRF Policy",
   "browser.ssrfPolicy.dangerouslyAllowPrivateNetwork": "Browser Dangerously Allow Private Network",
   "browser.ssrfPolicy.allowedHostnames": "Browser Allowed Hostnames",
+  "browser.ssrfPolicy.blockedHostnames": "Browser Blocked Hostnames",
   "browser.ssrfPolicy.allowRfc2544BenchmarkRange": "Browser Allow RFC 2544 Benchmark Range",
   "browser.ssrfPolicy.allowIpv6UniqueLocalRange": "Browser Allow IPv6 Unique Local Range",
   "tools.exec.timeoutSeconds": "Exec Timeout (Seconds)",
diff --git a/src/config/types.browser.ts b/src/config/types.browser.ts
index 88a10323..8bbe72a6 100644
--- a/src/config/types.browser.ts
+++ b/src/config/types.browser.ts
@@ -37,7 +37,10 @@ export type BrowserExtensionRelayConfig = {
   /** Temporarily accept legacy relay bearer/basic/subprotocol auth. Default: true. */
   allowLegacyAuth?: boolean;
 };
-export type BrowserSsrFPolicyConfig = SsrFPolicyConfig;
+export type BrowserSsrFPolicyConfig = SsrFPolicyConfig & {
+  /** Hostnames blocked for browser navigation, including "*.example.com" subdomain patterns. */
+  blockedHostnames?: string[];
+};
 export type BrowserConfig = {
   /** @deprecated Doctor-only legacy input; canonical schema rejects this field. */
   color?: string;
diff --git a/src/config/zod-schema.root-shape.ts b/src/config/zod-schema.root-shape.ts
index d30bad7e..2c12130d 100644
--- a/src/config/zod-schema.root-shape.ts
+++ b/src/config/zod-schema.root-shape.ts
@@ -33,6 +33,10 @@ import {
 } from "./zod-schema.root-support.js";
 import { sensitive } from "./zod-schema.sensitive.js";
 import { CommandsSchema, MessagesSchema, SessionSchema } from "./zod-schema.session.js";
+
+const BrowserSsrFPolicyConfigSchema = SsrFPolicyConfigSchema.extend({
+  blockedHostnames: z.array(z.string()).optional(),
+});
 import { TelemetryConfigSchema } from "./zod-schema.telemetry.js";

 // OpenTelemetry instrument names start with an ASCII letter and allow only these characters.
@@ -156,7 +160,7 @@ export const OpenClawSchemaShape = {
       attachOnly: z.boolean().optional(),
       defaultProfile: z.string().optional(),
       snapshotDefaults: BrowserSnapshotDefaultsSchema,
-      ssrfPolicy: SsrFPolicyConfigSchema.optional(),
+      ssrfPolicy: BrowserSsrFPolicyConfigSchema.optional(),
       profiles: z
         .record(
           z.string().regex(/^[a-z0-9-]+$/, "Profile names must be alphanumeric with hyphens only"),
diff --git a/src/infra/net/ssrf.pinning.test.ts b/src/infra/net/ssrf.pinning.test.ts
index e432d78e..014c45cb 100644
--- a/src/infra/net/ssrf.pinning.test.ts
+++ b/src/infra/net/ssrf.pinning.test.ts
@@ -214,6 +214,30 @@ describe("ssrf pinning", () => {
     expect(lookup).not.toHaveBeenCalled();
   });

+  it("blocks configured hostnames before DNS lookup", async () => {
+    const lookup = createPublicLookupMock();
+
+    await expect(
+      resolvePinnedHostnameWithPolicy("www.baidu.com", {
+        lookupFn: lookup,
+        policy: { blockedHostnames: ["www.baidu.com"] },
+      }),
+    ).rejects.toThrow(/blocked hostname/i);
+    expect(lookup).not.toHaveBeenCalled();
+  });
+
+  it("supports wildcard hostname blocklist patterns", async () => {
+    const lookup = createPublicLookupMock();
+
+    await expect(
+      resolvePinnedHostnameWithPolicy("ads.example.com", {
+        lookupFn: lookup,
+        policy: { blockedHostnames: ["*.example.com"] },
+      }),
+    ).rejects.toThrow(/blocked hostname/i);
+    expect(lookup).not.toHaveBeenCalled();
+  });
+
   it("supports wildcard hostname allowlist patterns", async () => {
     const lookup = vi.fn(async () => [
       { address: "93.184.216.34", family: 4 },
diff --git a/src/infra/net/ssrf.test.ts b/src/infra/net/ssrf.test.ts
index 6b09c7ac..18d28d6e 100644
--- a/src/infra/net/ssrf.test.ts
+++ b/src/infra/net/ssrf.test.ts
@@ -355,6 +355,7 @@ describe("isSameSsrFPolicy", () => {
           allowedOrigins: ["https://A.example.com/v1", "https://b.example.com"],
           allowedHostnames: ["b.example.com", "A.example.com"],
           hostnameAllowlist: ["*.example.com", "api.example.com"],
+          blockedHostnames: ["tracker.example.com", "*.ads.example.com"],
         },
         {
           allowPrivateNetwork: true,
@@ -362,10 +363,18 @@ describe("isSameSsrFPolicy", () => {
           allowedOrigins: ["https://b.example.com", "https://a.example.com/other"],
           allowedHostnames: ["a.example.com", "B.EXAMPLE.COM"],
           hostnameAllowlist: ["api.example.com", "*.example.com"],
+          blockedHostnames: ["*.ads.example.com", "TRACKER.example.com"],
         },
       ),
     ).toBe(true);

+    expect(
+      isSameSsrFPolicy(
+        { blockedHostnames: ["tracker.example.com"] },
+        { blockedHostnames: ["analytics.example.com"] },
+      ),
+    ).toBe(false);
+
     expect(
       isSameSsrFPolicy(
         { dangerouslyAllowPrivateNetwork: true },
diff --git a/src/infra/net/ssrf.ts b/src/infra/net/ssrf.ts
index 5e175a98..362deb0e 100644
--- a/src/infra/net/ssrf.ts
+++ b/src/infra/net/ssrf.ts
@@ -65,6 +65,7 @@ export type SsrFPolicy = {
    */
   allowedOrigins?: string[];
   hostnameAllowlist?: string[];
+  blockedHostnames?: string[];
 };

 function normalizeSsrFPolicyHostnames(values?: string[]): string[] {
@@ -89,6 +90,7 @@ function normalizeSsrFPolicyForComparison(policy?: SsrFPolicy) {
     allowedHostnames: normalizeSsrFPolicyHostnames(policy.allowedHostnames),
     allowedOrigins: normalizeSsrFPolicyOrigins(policy.allowedOrigins),
     hostnameAllowlist: [...normalizeHostnameAllowlist(policy.hostnameAllowlist)].toSorted(),
+    blockedHostnames: [...normalizeHostnameAllowlist(policy.blockedHostnames)].toSorted(),
   };
 }

@@ -134,6 +136,11 @@ export function mergeSsrFPolicies(
         new Set([...(merged.hostnameAllowlist ?? []), ...policy.hostnameAllowlist]),
       );
     }
+    if (policy.blockedHostnames?.length) {
+      merged.blockedHostnames = Array.from(
+        new Set([...(merged.blockedHostnames ?? []), ...policy.blockedHostnames]),
+      );
+    }
   }
   return Object.keys(merged).length > 0 ? merged : undefined;
 }
@@ -236,6 +243,11 @@ function shouldSkipPrivateNetworkChecks(hostname: string, policy?: SsrFPolicy):
   );
 }

+function isHostnameBlockedByPolicy(hostname: string, policy?: SsrFPolicy): boolean {
+  const blockedHostnames = normalizeHostnameAllowlist(policy?.blockedHostnames);
+  return blockedHostnames.length > 0 && matchesHostnameAllowlist(hostname, blockedHostnames);
+}
+
 export function resolveSsrFPolicyForUrl(url: URL, policy?: SsrFPolicy): SsrFPolicy | undefined {
   // allowedOrigins trust only the current request hostname for the matching
   // origin; redirects must re-evaluate against their own URL.
@@ -386,6 +398,10 @@ function resolveHostnamePolicyChecks(
   const hostnameAllowlist = normalizeHostnameAllowlist(policy?.hostnameAllowlist);
   const skipPrivateNetworkChecks = shouldSkipPrivateNetworkChecks(normalized, policy);

+  if (isHostnameBlockedByPolicy(normalized, policy)) {
+    throw new SsrFBlockedError(`Blocked hostname (configured blocklist): ${hostname}`);
+  }
+
   if (!matchesHostnameAllowlist(normalized, hostnameAllowlist)) {
     throw new SsrFBlockedError(
       `Domain policy: Blocked hostname (not in allowlist): ${hostname}. Permitted hostname patterns: ${hostnameAllowlist.join(", ")}. Try a URL on a permitted domain.`,
